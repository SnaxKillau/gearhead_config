management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    app:
      name: Gateway Server Microservice
      description: Gateway Server Application
      version: 1.0.0
    env:
      enabled: true
  endpoint:
    gateway:
      enabled: true
